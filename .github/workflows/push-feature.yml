name: feature [push]

on:
  push:
    branches:
      - 'feature/**'

concurrency:
  group: ${{ github.workflow }}-${{ github.event.number || github.ref }}
  cancel-in-progress: true

jobs:
  main:
    name: Nx Cloud - Main Job
    uses: nrwl/ci/.github/workflows/nx-cloud-main.yml@v0.14.0
    with:
      parallel-commands: |
        npx nx format:check --no-cloud flag
      parallel-commands-on-agents: |
        npx nx affected --target=lint --parallel=3 --no-cloud flag
        npx nx affected --target=test --parallel=3 --exclude='tag:puppeteer' --ci --code-coverage --no-cloud flag

  agents:
    name: Nx Cloud - Agents
    uses: nrwl/ci/.github/workflows/nx-cloud-agents.yml@v0.14.0
    with:
      number-of-agents: 3
